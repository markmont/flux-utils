
# For the following accounts, allow the specified users to use
# the LSA public allocations even if they are on the userlist for
# these accounts.

ignore_accounts = {
     'support_flux': [ 'markmont', 'mrchampe', 'cja', 'nbrack', 'smeyer' ]
}

